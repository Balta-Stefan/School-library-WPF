<Window x:Class="School_library.Views.AddNewLoanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:School_library.Views"
        mc:Ignorable="d"
        Title="AddNewLoanWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1">
            <TextBlock>Member</TextBlock>
            <ComboBox ItemsSource="{Binding Members}" SelectedItem="{Binding SelectedMember}" IsTextSearchEnabled="True"/>

            <TextBlock>Book</TextBlock>
            <ComboBox ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}"/>

            <TextBlock>Copy</TextBlock>
            <ComboBox ItemsSource="{Binding Copies}" SelectedItem="{Binding SelectedCopy}"/>

            <Button Padding="13,13,13,13" Margin="0,20,0,0" Background="#700101" Foreground="White" FontWeight="Bold" Command="{Binding addLoanCommand}">Add loan</Button>
        </StackPanel>
    </Grid>
</Window>
