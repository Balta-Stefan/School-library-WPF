<UserControl x:Class="School_library.Views.BooksPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:School_library.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="1" HorizontalAlignment="Right" Padding="13,13,13,13" Background="#700101" Foreground="White" FontWeight="Bold">Add new book</Button>

        <Grid Grid.Row="0" Margin="15,15,15,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.3*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="0.3*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0">
                    <TextBlock>Name</TextBlock>
                    <TextBox/>
                    <TextBlock>Publisher</TextBlock>
                    <ListBox ItemsSource="{Binding Publishers}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0">
                    <TextBlock>Genre</TextBlock>
                    <ListBox ItemsSource="{Binding Genres}"/>
                    <TextBlock>Number of copies</TextBlock>
                    <TextBox/>
                </StackPanel>

                <Button Grid.Column="1" Grid.Row="1" Background="Red" Foreground="White" FontWeight="Bold">Search</Button>
            </Grid>

            <ListBox Grid.Row="1" ItemsSource="{Binding Books}" HorizontalContentAlignment="Stretch" Background="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Grid}, Path=Background}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="white"/>
                        <Setter Property="Padding" Value="35,35,35,35"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="yellow"/>

                            <StackPanel Grid.Column="1">
                                <WrapPanel>
                                    <TextBlock Text="Title: "/>
                                    <TextBlock Text="{Binding bookTitle}"/>
                                </WrapPanel>
                                <WrapPanel>
                                    <TextBlock Text="Author: "/>
                                    <TextBlock Text="{Binding author.firstName}"/>
                                </WrapPanel>
                            </StackPanel>

                            <Button Grid.Column="2" Background="Red" Foreground="White" FontWeight="Bold">Change</Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
